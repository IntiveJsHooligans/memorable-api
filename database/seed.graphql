mutation {
  first: createTaskStatus(data: {
    name: "Active"
  }) {
    id
  }
  
  second: createTaskStatus(data: {
    name: "Inactive"
  }) {
    id
  }
  
  third: createTaskStatus(data: {
    name: "Pending"
  }) {
    id
  }
}